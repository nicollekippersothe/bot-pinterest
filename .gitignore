node_modules
.vercel

# Engine de afiliados
.env
engine/dist
storage/*
!storage/.gitkeep
*.db
*.db-shm
*.db-wal

# Pins gerados: publicados via deploy, não versionados por padrão
public/pins/
public/feed.json
